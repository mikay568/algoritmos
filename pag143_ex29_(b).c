#include<stdio.h>

void main ()
{float N;
printf("escreva um numero positivo ou negativo\n");
    scanf("%f", &N);
    if(N>0)
   printf("numero positivo");
  if(N<0)
    printf("numero negativo");
}
